import React, { PureComponent } from 'react';
import ProjectList from './ProjectList';
export default class ActivityList extends PureComponent {
  render() {
    return (<ProjectList type={1}/>);
  }
}
