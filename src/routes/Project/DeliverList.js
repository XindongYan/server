import React, { PureComponent } from 'react';
import ProjectList from './ProjectList';
export default class DeliverList extends PureComponent {
  render() {
    return (<ProjectList type={2}/>);
  }
}
